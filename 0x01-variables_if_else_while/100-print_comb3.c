#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int c, d;

	c = 48;
	d = 48;

	while (d < 58)
	{
		c = 48;
		while (c < 58)
		{
			if (d != i && e < c)
			{
				putchar(d);
				putchar(c);
				if (c == 57 && d == 56)
				{
					break;
				}
				putchar(',');
				putchar(' ');
			}
			c++;
		}
		d++;
	}
	putchar('\n');
	return (0);
}
